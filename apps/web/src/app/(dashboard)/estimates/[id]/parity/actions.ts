"use server";import{redirect}from"next/navigation";import{revalidatePath}from"next/cache";import{currentActor}from"@/auth/current-actor";import{EstimateParityService}from"@/modules/estimates/parity.service";export async function reviseAction(id:string,data:FormData){const actor=await currentActor();await new EstimateParityService().createRevision(actor!,id,{reason:String(data.get("reason")??"")||undefined});revalidatePath(`/estimates/${id}`);redirect(`/estimates/${id}`)}export async function shareAction(id:string){const actor=await currentActor();const{token}=await new EstimateParityService().issuePublicLink(actor!,id);redirect(`/proposal/${token}`)}
