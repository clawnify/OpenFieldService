import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{EstimateService}from"@/modules/estimates/estimate.service";import{reviseAction,shareAction}from"./actions";export default async function Page({params}:{params:Promise<{id:string}>}){const actor=await currentActor();if(!actor)redirect("/login");const{id}=await params,view=await new EstimateService().get(actor,id);return <main className="mx-auto max-w-3xl px-6 py-12"><h1 className="text-3xl font-semibold">Proposal controls</h1><p className="mt-2">{view.quote.identifier} · {view.quote.status} · version {view.version.versionNumber}</p><div className="mt-8 flex gap-3"><form action={shareAction.bind(null,id)}><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground">Issue secure proposal link</button></form><a className="rounded-md border px-4 py-2" href={`/api/estimates/${id}/pdf`}>Proposal PDF</a></div>{["sent","rejected","expired","cancelled"].includes(view.quote.status)?<form action={reviseAction.bind(null,id)} className="mt-8 flex gap-3"><input name="reason" placeholder="Revision reason" className="flex-1 rounded-md border px-3"/><button className="rounded-md border px-4 py-2">Create revision</button></form>:null}</main>}
