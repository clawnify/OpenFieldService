import Link from"next/link";import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{CustomerService}from"@/modules/customers/customer.service";import{EstimateService}from"@/modules/estimates/estimate.service";import{createEstimateAction}from"./actions";export default async function Page(){const actor=await currentActor();if(!actor)redirect("/login");const[q,customers]=await Promise.all([new EstimateService().list(actor,{page:1,pageSize:50}),new CustomerService().listCustomers(actor,{page:1,pageSize:100})]);return <main className="mx-auto max-w-6xl px-6 py-12"><p className="text-sm text-primary">Commercial proposals</p><h1 className="text-4xl font-semibold">Estimates</h1><div className="mt-6 space-y-2">{q.items.map(x=><Link className="block rounded-xl border p-4" href={`/estimates/${x.id}`} key={x.id}><b>{x.identifier}</b><span className="ml-3 text-muted-foreground">{x.status}</span></Link>)}</div><form action={createEstimateAction} className="mt-8 grid gap-3 rounded-xl border p-5 md:grid-cols-3"><h2 className="text-xl font-semibold md:col-span-3">New Good / Better / Best estimate</h2><select required name="customerId" className="rounded-md border px-3"><option value="">Customer</option>{customers.items.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select><input type="date" name="expiresOn" className="rounded-md border px-3"/><input type="number" name="taxBasisPoints" defaultValue="1200" min="0" max="10000" className="rounded-md border px-3"/><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground md:col-span-3">Create estimate</button></form></main>}
