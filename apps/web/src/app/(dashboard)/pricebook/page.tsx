import{redirect}from"next/navigation";import{can}from"@/auth/permissions";import{currentActor}from"@/auth/current-actor";import{PricebookService}from"@/modules/pricebook/pricebook.service";import{archivePricebookItemAction,createPricebookItemAction}from"./actions";export default async function Page({searchParams}:{searchParams:Promise<{query?:string}>}){const actor=await currentActor();if(!actor)redirect("/login");const q=await searchParams;const result=await new PricebookService().listItems(actor,{query:q.query,page:1,pageSize:50});return <main className="mx-auto max-w-6xl px-6 py-12"><p className="text-sm text-primary">Commercial</p><h1 className="text-4xl font-semibold">Pricebook</h1><form className="mt-6 flex gap-2"><input name="query" defaultValue={q.query} placeholder="Name or SKU" className="flex-1 rounded-md border px-3 py-2"/><button className="rounded-md border px-4">Search</button></form><table className="mt-6 w-full rounded-xl border text-left text-sm"><thead><tr><th className="p-3">Item</th><th>Type</th><th>Sell price</th><th>Status</th><th/></tr></thead><tbody>{result.items.map(i=><tr className="border-t" key={i.id}><td className="p-3">{i.name}<div className="text-xs text-muted-foreground">{i.sku}</div></td><td>{i.type}</td><td>{(i.sellPriceCents/100).toFixed(2)} CAD</td><td>{i.status}</td><td>{can(actor,"pricebook.manage")&&i.status==="active"?<form action={archivePricebookItemAction.bind(null,i.id)}><button>Archive</button></form>:null}</td></tr>)}</tbody></table>{can(actor,"pricebook.manage")?<form action={createPricebookItemAction} className="mt-8 grid gap-3 rounded-xl border p-5 md:grid-cols-3"><h2 className="text-xl font-semibold md:col-span-3">New item</h2><input required name="name" placeholder="Name" className="rounded-md border px-3 py-2"/><input name="sku" placeholder="SKU" className="rounded-md border px-3"/><select name="type" className="rounded-md border px-3"><option>service</option><option>labor</option><option>material</option><option>equipment</option><option>part</option><option>other</option></select><input name="unit" defaultValue="each" className="rounded-md border px-3"/><input name="costCents" type="number" min="0" placeholder="Cost cents" className="rounded-md border px-3"/><input name="sellPriceCents" type="number" min="0" placeholder="Sell cents" className="rounded-md border px-3"/><label><input name="taxable" type="checkbox" defaultChecked/> Taxable</label><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground">Create</button></form>:null}</main>}
