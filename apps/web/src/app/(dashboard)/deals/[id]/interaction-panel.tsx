import type { RequestActor } from "@/modules/customers/customer.service";
import { TimelineService } from "@/modules/interactions/timeline.service";
import { addDealActivityAction, addDealNoteAction } from "./interaction-actions";
export async function InteractionPanel({actor,dealId}:{actor:RequestActor;dealId:string}){const timeline=await new TimelineService().list(actor,{targetType:"deal",targetId:dealId});return <section className="mt-10"><h2 className="text-xl font-semibold">Activity & notes</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><form action={addDealActivityAction.bind(null,dealId)} className="space-y-2 rounded-lg border p-4"><select name="type" className="w-full rounded-md border bg-background px-3 py-2"><option>call</option><option>email</option><option>meeting</option><option>visit</option><option>other</option></select><input required name="subject" placeholder="Activity subject" className="w-full rounded-md border bg-background px-3 py-2"/><textarea name="details" placeholder="Details" className="w-full rounded-md border bg-background px-3 py-2"/><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground">Add activity</button></form><form action={addDealNoteAction.bind(null,dealId)} className="space-y-2 rounded-lg border p-4"><textarea required name="body" placeholder="Internal note" className="min-h-24 w-full rounded-md border bg-background px-3 py-2"/><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground">Add note</button></form></div><ol className="mt-4 space-y-2">{timeline.map((item)=><li key={`${item.kind}-${item.id}`} className="rounded-lg border p-3"><div className="flex justify-between"><strong>{item.title}</strong><span className="text-xs text-muted-foreground">{item.timestamp.toISOString()}</span></div>{item.details?<p className="mt-1 whitespace-pre-wrap text-sm">{item.details}</p>:null}<span className="text-xs uppercase text-muted-foreground">{item.kind}</span></li>)}</ol></section>}
