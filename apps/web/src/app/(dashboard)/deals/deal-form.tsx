"use client";
import { useActionState, useState } from "react";
import { createDealAction } from "./actions";
export interface DealPipelineChoice { id: string; name: string; stages: Array<{ id: string; name: string; kind: string }> }
export function DealForm({ pipelines, customers }: { pipelines: DealPipelineChoice[]; customers: Array<{ id: string; name: string }> }) { const [state, action, pending] = useActionState(createDealAction, {}); const [pipelineId, setPipelineId] = useState(pipelines[0]?.id ?? ""); const pipeline = pipelines.find((value) => value.id === pipelineId); const openStages = pipeline?.stages.filter((stage) => stage.kind === "open") ?? []; return <form action={action} className="mt-10 grid gap-4 rounded-xl border bg-card p-6 sm:grid-cols-2"><h2 className="text-xl font-semibold sm:col-span-2">Create deal</h2><label className="text-sm">Name<input required name="name" className="mt-1 w-full rounded-md border bg-background px-3 py-2" /></label><label className="text-sm">Customer<select required name="customerId" className="mt-1 w-full rounded-md border bg-background px-3 py-2">{customers.map((customer) => <option value={customer.id} key={customer.id}>{customer.name}</option>)}</select></label><label className="text-sm">Pipeline<select required name="pipelineId" value={pipelineId} onChange={(event) => setPipelineId(event.target.value)} className="mt-1 w-full rounded-md border bg-background px-3 py-2">{pipelines.map((value) => <option value={value.id} key={value.id}>{value.name}</option>)}</select></label><label className="text-sm">Opening stage<select required name="stageId" className="mt-1 w-full rounded-md border bg-background px-3 py-2">{openStages.map((stage) => <option value={stage.id} key={stage.id}>{stage.name}</option>)}</select></label><label className="text-sm">Value in cents<input type="number" min="0" step="1" name="amountCents" defaultValue="0" className="mt-1 w-full rounded-md border bg-background px-3 py-2" /></label><label className="text-sm">Currency<input name="currency" defaultValue="CAD" maxLength={3} className="mt-1 w-full rounded-md border bg-background px-3 py-2 uppercase" /></label><label className="text-sm">Expected close<input type="date" name="expectedCloseDate" className="mt-1 w-full rounded-md border bg-background px-3 py-2" /></label><label className="text-sm">Source<input name="source" className="mt-1 w-full rounded-md border bg-background px-3 py-2" /></label>{state.error ? <p className="text-sm text-destructive sm:col-span-2">{state.error}</p> : null}<button disabled={pending || !openStages.length || !customers.length} className="rounded-md bg-primary px-4 py-2 text-primary-foreground disabled:opacity-50 sm:col-span-2">{pending ? "Creating…" : "Create deal"}</button></form>; }
