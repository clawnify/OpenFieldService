"use client";
import { useActionState } from "react";
import { createPipelineAction } from "./actions";
export function PipelineForm() { const [state, action, pending] = useActionState(createPipelineAction, {}); return <form action={action} className="space-y-4 rounded-xl border bg-card p-6"><h2 className="text-xl font-semibold">Create pipeline</h2><label className="block text-sm">Name<input required name="name" className="mt-1 w-full rounded-md border bg-background px-3 py-2" /></label><label className="block text-sm">Description<textarea name="description" className="mt-1 min-h-20 w-full rounded-md border bg-background px-3 py-2" /></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" name="makeDefault" />Make default</label>{state.error ? <p className="text-sm text-destructive">{state.error}</p> : null}<button disabled={pending} className="rounded-md bg-primary px-4 py-2 text-primary-foreground disabled:opacity-50">{pending ? "Creating…" : "Create pipeline"}</button></form>; }
