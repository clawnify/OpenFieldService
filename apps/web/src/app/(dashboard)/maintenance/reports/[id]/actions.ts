"use server";import{revalidatePath}from"next/cache";import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{MaintenanceParityService}from"@/modules/maintenance";export async function submitReportAction(id:string,data:FormData){const actor=await currentActor();if(!actor)redirect("/login");await new MaintenanceParityService().submitServiceReport(actor,id,{responses:{completed:String(data.get("completed"))==="true"},findings:String(data.get("findings")??""),deficiencies:String(data.get("deficiencies")??""),recommendations:String(data.get("recommendations")??""),preWorkAttachmentId:String(data.get("preWorkAttachmentId")??""),postWorkAttachmentId:String(data.get("postWorkAttachmentId")??""),customerSignerName:String(data.get("customerSignerName")??""),customerSignature:String(data.get("customerSignature")??""),expectedRowVersion:Number(data.get("expectedRowVersion")??0)});revalidatePath(`/maintenance/reports/${id}`)}export async function completeVisitAction(id:string){const actor=await currentActor();if(!actor)redirect("/login");await new MaintenanceParityService().completeMaintenanceVisit(actor,id);revalidatePath(`/maintenance/reports/${id}`)}
