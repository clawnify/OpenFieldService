import Link from"next/link";import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{MaintenanceParityService}from"@/modules/maintenance";export default async function Page(){const actor=await currentActor();if(!actor)redirect("/login");const reports=await new MaintenanceParityService().listServiceReports(actor);return <main className="mx-auto max-w-5xl px-6 py-12"><p className="text-sm text-primary">Completion evidence</p><h1 className="text-4xl font-semibold">Maintenance service reports</h1><div className="mt-8 grid gap-3">{reports.map(r=><Link href={`/maintenance/reports/${r.id}`} className="rounded-xl border p-4" key={r.id}><b>{r.status}</b><p className="text-sm text-muted-foreground">Job {r.jobId} · {r.submittedAt?.toISOString()??"not submitted"}</p></Link>)}</div></main>}
