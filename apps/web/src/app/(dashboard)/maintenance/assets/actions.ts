"use server";import{revalidatePath}from"next/cache";import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{MaintenanceParityService}from"@/modules/maintenance";export async function createAssetAction(data:FormData){const actor=await currentActor();if(!actor)redirect("/login");await new MaintenanceParityService().createAsset(actor,{customerId:String(data.get("customerId")??""),type:String(data.get("type")??""),displayName:String(data.get("displayName")??""),manufacturer:String(data.get("manufacturer")??""),model:String(data.get("model")??""),serialNumber:String(data.get("serialNumber")??""),serviceLocation:String(data.get("serviceLocation")??"")});revalidatePath("/maintenance/assets")}
