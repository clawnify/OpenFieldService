import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{CustomerService}from"@/modules/customers/customer.service";import{MaintenanceParityService}from"@/modules/maintenance";import{createAssetAction}from"./actions";export default async function Page(){const actor=await currentActor();if(!actor)redirect("/login");const service=new MaintenanceParityService(),[assets,customers]=await Promise.all([service.listAssets(actor),new CustomerService().listCustomers(actor,{page:1,pageSize:100})]);return <main className="mx-auto max-w-5xl px-6 py-12"><p className="text-sm text-primary">Maintenance configuration</p><h1 className="text-4xl font-semibold">Assets / equipment</h1><div className="mt-6 grid gap-3">{assets.map(a=><div className="rounded-xl border p-4" key={a.id}><b>{a.displayName||a.type}</b><p className="text-sm text-muted-foreground">{a.manufacturer} {a.model} · {a.serialNumber||"No serial"} · {a.status}</p></div>)}</div><form action={createAssetAction} className="mt-8 grid gap-3 rounded-xl border p-5 md:grid-cols-2"><h2 className="text-xl font-semibold md:col-span-2">Add equipment</h2><select required name="customerId" className="rounded-md border px-3 py-2"><option value="">Customer</option>{customers.items.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input required name="type" placeholder="Equipment type" className="rounded-md border px-3 py-2"/><input name="displayName" placeholder="Display name" className="rounded-md border px-3 py-2"/><input name="manufacturer" placeholder="Manufacturer" className="rounded-md border px-3 py-2"/><input name="model" placeholder="Model" className="rounded-md border px-3 py-2"/><input name="serialNumber" placeholder="Serial number" className="rounded-md border px-3 py-2"/><input required name="serviceLocation" placeholder="Service location" className="rounded-md border px-3 py-2 md:col-span-2"/><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground md:col-span-2">Create asset</button></form></main>}
