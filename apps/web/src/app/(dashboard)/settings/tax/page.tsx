import{redirect}from"next/navigation";import{currentActor}from"@/auth/current-actor";import{TaxService}from"@/modules/estimates/tax.service";import{publishTaxAction}from"./actions";export default async function Page(){const actor=await currentActor();if(!actor)redirect("/login");const current=await new TaxService().current(actor);return <main className="mx-auto max-w-3xl px-6 py-12"><p className="text-sm text-primary">Settings</p><h1 className="text-4xl font-semibold">Tax profile</h1>{current?<p className="mt-3 text-muted-foreground">{current.countryCode}-{current.regionCode} · {current.pricesIncludeTax?"Tax-inclusive":"Tax-exclusive"} · {current.components.map(x=>`${x.code} ${x.rateBasisPoints/100}%`).join(", ")}</p>:<p className="mt-3">Tax is not configured.</p>}<form action={publishTaxAction} className="mt-8 grid gap-3 rounded-xl border p-5 md:grid-cols-2"><label><input type="checkbox" name="enabled" defaultChecked/> Tax enabled</label><label><input type="checkbox" name="pricesIncludeTax"/> Prices include tax</label><input name="countryCode" defaultValue="CA" className="rounded-md border px-3 py-2"/><input name="regionCode" defaultValue="BC" className="rounded-md border px-3 py-2"/><input name="currency" defaultValue="CAD" className="rounded-md border px-3 py-2"/><span/><input name="code1" defaultValue="GST" className="rounded-md border px-3 py-2"/><input name="rate1" type="number" defaultValue="500" className="rounded-md border px-3 py-2"/><input name="code2" defaultValue="PST" className="rounded-md border px-3 py-2"/><input name="rate2" type="number" defaultValue="700" className="rounded-md border px-3 py-2"/><input type="hidden" name="name1" value="GST"/><input type="hidden" name="name2" value="PST"/><button className="rounded-md bg-primary px-4 py-2 text-primary-foreground md:col-span-2">Publish new profile version</button></form></main>}
