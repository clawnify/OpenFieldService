"use server";import{headers}from"next/headers";import{redirect}from"next/navigation";import{ApplicationError}from"@/lib/errors";import{EstimateParityService}from"@/modules/estimates/parity.service";export async function selectProposalAction(token:string,data:FormData){const h=await headers();try{await new EstimateParityService().selectPublic(token,{optionId:String(data.get("optionId")??""),selectorName:String(data.get("selectorName")??"")},{ip:h.get("x-forwarded-for")?.split(",")[0]?.trim()??null,userAgent:h.get("user-agent")});}catch(error){if(error instanceof ApplicationError)redirect(`/proposal/${encodeURIComponent(token)}?error=1`);throw error;}redirect(`/proposal/${token}?accepted=1`)}
