import{calculateTax,type TaxComponentInput}from"./tax.rules";export function priceOptionWithProfile(lines:{amountCents:number;taxable:boolean}[],discountCents:number,profile:{enabled:boolean;pricesIncludeTax:boolean;components:TaxComponentInput[]}){const gross=lines.reduce((n,x)=>n+x.amountCents,0),taxableGross=lines.filter(x=>x.taxable).reduce((n,x)=>n+x.amountCents,0),taxableDiscount=gross?Math.round(discountCents*taxableGross/gross):0;const tax=calculateTax([{amountCents:Math.max(0,taxableGross-taxableDiscount),taxable:true},{amountCents:Math.max(0,gross-taxableGross-(discountCents-taxableDiscount)),taxable:false}],profile);return{subtotalCents:profile.pricesIncludeTax?gross-tax.totalTaxCents:gross,discountCents,taxCents:tax.totalTaxCents,totalCents:tax.totalCents,tax}}
