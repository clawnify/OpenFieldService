import{describe,expect,it}from"vitest";import{ConflictError}from"@/lib/errors";import{assertEstimateTransition,calculateOption,lineTotalCents}from"./estimate.rules";describe("estimate pricing and lifecycle",()=>{it("uses exact thousandth quantity rounding",()=>expect(lineTotalCents(1250,199)).toBe(249));it("calculates discount then taxable share and tax",()=>expect(calculateOption([{quantityMilli:1000,unitPriceCents:10000,taxable:true},{quantityMilli:1000,unitPriceCents:5000,taxable:false}],{type:"percent",basisPoints:1000,inputCents:0},1200)).toEqual({subtotalCents:15000,discountCents:1500,taxCents:1080,totalCents:14580}));it("caps fixed discounts",()=>expect(calculateOption([{quantityMilli:1000,unitPriceCents:100,taxable:true}],{type:"fixed",basisPoints:0,inputCents:500},500)).toEqual({subtotalCents:100,discountCents:100,taxCents:0,totalCents:0}));it("enforces terminal lifecycle and reasons",()=>{expect(()=>assertEstimateTransition("draft","sent")).not.toThrow();expect(()=>assertEstimateTransition("sent","rejected")).toThrow(ConflictError);expect(()=>assertEstimateTransition("accepted","sent")).toThrow(ConflictError)})});
